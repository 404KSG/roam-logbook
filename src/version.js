/** Release and persisted-format identifiers shown in development documentation. */
export const PLUGIN_VERSION = '0.9.0-beta.9';
export const STATE_FORMATS = Object.freeze({
    pauseBatch: 2,
    pomodoroTargets: 1,
    stateBackups: 1,
});
